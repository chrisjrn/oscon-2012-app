<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.s31.oscon"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="15" />

    <application
        android:name=".OsconApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".OsconAppActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".DayListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScheduleItemActivity"
            android:label="@string/app_name" >
        </activity>

        <provider
            android:name=".schedule.ScheduleProvider"
            android:authorities="org.s31.oscon.schedule.scheduleprovider" />
    </application>

</manifest>